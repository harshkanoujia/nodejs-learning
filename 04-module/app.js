// var logger = require('./logger') //we not prefer to use var because of we never want that its value can change 
const log = require('./logger');  //we prefer const because we want that its value can not be change

// console.log(log);
log('message');